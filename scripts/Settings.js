var timeBetweenMating = 1000;
var maxPopulation = 30;
var wallElasticityAmount = 1;

$(".time-between-matings").text(timeBetweenMating);
$(".max-amount").text(maxPopulation);
$(".wall-elasticity").text(wallElasticityAmount);
